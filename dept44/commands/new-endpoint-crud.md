---
description: "Scaffold a new dept44 CRUD endpoint backed by a local database (model, entity, repo, mapper, service, resource, migration, tests, OpenAPI)"
argument-hint: "<EntityName> [with field:Type, field:Type, ...]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/dept44-scaffold/references/new-endpoint-crud.md` and follow it step by step to scaffold the full CRUD endpoint described below — every file it lists, including all test classes and the OpenAPI spec changes. Examine existing code in the repo first to match its exact import style, naming, and formatting. When done, run `mvn test` then `mvn verify`.

What to scaffold:
$ARGUMENTS
