---
description: "dept44 API model (POJO / DTO) pattern + bean test pattern"
argument-hint: "[optional: model class or notes]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/dept44-patterns/references/pattern-pojo.md` — the canonical dept44 API model pattern (`create()` / `with*()` / manual `equals`/`hashCode`/`toString`, `@Schema`, validation, no Lombok, no enums) and its three-test bean pattern — and follow it exactly when writing, reviewing, or fixing `api/model/` classes. Look at existing models in the repo for exact style.

$ARGUMENTS
