---
description: "dept44 REST controller (Resource) pattern + Resource / FailureTest test patterns"
argument-hint: "[optional: Resource class or notes]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/dept44-patterns/references/pattern-resource.md` — the canonical dept44 Resource (controller) pattern, including the happy-path `{Resource}Test` and the `{Resource}FailureTest` validation test — and follow it exactly when writing, reviewing, or fixing the `api/` layer. Look at existing Resources in the repo for exact style.

$ARGUMENTS
