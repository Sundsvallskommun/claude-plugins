---
description: "Scaffold a new dept44 Feign client integration to an external service (client, configuration, properties, config, tests)"
argument-hint: "<externalServiceName>"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/dept44-scaffold/references/new-integration.md` and follow it step by step to scaffold the integration described below — the Feign `{Service}Client` (with `@CircuitBreaker`), `{Service}Configuration`, `{Service}Properties`, the `application.yml` / `application-junit.yml` entries, the OpenAPI spec + generator execution, and the three test classes. Examine existing integrations in the repo first to match exact style.

What to scaffold:
$ARGUMENTS
